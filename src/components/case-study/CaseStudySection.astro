---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<section class="flex flex-col gap-2.5">
  <h4 class="text-base font-semibold leading-snug text-neutral-600">{label}</h4>
  <div class="flex flex-col gap-5">
    <slot />
  </div>
</section>
